# Interim ANTAQ

Nenhuma camada é publicada enquanto cadastro, coordenadas e movimentações oficiais não forem obtidos e validados. O esquema de saída está definido em `ice_sul.transform.antaq.OUTPUT_FIELDS`.
